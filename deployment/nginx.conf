# LaserSim production reverse proxy configuration
server {
    listen 80;
    server_name lasersim.local;

    location / {
        proxy_pass http://lasersim-api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
