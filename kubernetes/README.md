# LaserSim Kubernetes Deployment

This directory contains the Kubernetes deployment foundation for the K-mode prediction API.

Components:

- Deployment: manages API replicas
- Service: internal service discovery
- Future additions:
  - GPU node scheduling
  - Horizontal Pod Autoscaling
  - ConfigMaps and Secrets
  - Monitoring integration
